const express = require('express');
const router = express.Router();
const ContributorsController = require('../controllers/contributor.controller');

router.get('/contributors', ()=>{});

module.exports = router;