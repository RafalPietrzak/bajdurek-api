const express = require('express');
const router = express.Router();
const StoryUsersController = require('../controllers/storyUser.controller');

router.get('/story-user', ()=>{});

module.exports = router;